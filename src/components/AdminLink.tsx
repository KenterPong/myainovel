/**
 * 管理頁面連結組件
 */

import React from 'react';
import Link from 'next/link';

interface AdminLinkProps {
  className?: string;
}

export function AdminLink({ className = '' }: AdminLinkProps) {
  return null; // 移除 AI 生成歷史按鈕
}
